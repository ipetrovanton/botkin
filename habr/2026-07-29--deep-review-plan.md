# Глубокое ревью проекта botkin и план-промт для легковесной модели

> Дата начала: 2026-07-29 14:47
> Стек: Python 3.12.9, FastAPI, aiogram 3, Ollama (qwen3-vl:8b-instruct), instructor, sqlite-vec, uv

## Постановка

Пользователь попросил провести глубокое ревью всего проекта (код, тесты, скрипты,
конфиги, модели) и написать детальный пошаговый план устранения недостатков,
пригодный как промт для легковесной модели. Требования к результату: академический
код, разделение слоёв, библиотеки вместо самописного, без мёртвых зависимостей,
e2e-тесты с метриками (скорость, tokens/sec, размер контекста, структурный diff),
промты отдельно от кода, предложения по оптимизации моделей/RAG/альтернативным VLM,
проверка актуальной версии Python и зависимостей.

Критерий успеха: `REVIEW_PLAN.md` с фазами и проверяемыми шагами.

## Контекст и ограничения

- Окружение: macOS (ревью), боевое — Windows + WSL2 + Ollama.
- Проект: ~23 700 строк Python (src + tests + scripts), 113 файлов тестов.
- Ограничение: план — только документ, правки кода не выполнялись.

## План

1. Обследовать структуру (src/tests/scripts/конфиги) — размеры модулей, мусор.
2. Проверить зависимости: реальные импорты vs pyproject, актуальные версии в сети.
3. Проверить актуальный Python и совместимость.
4. Ревью промтов и альтернативных локальных VLM (по свежим бенчам).
5. Написать `REVIEW_PLAN.md`.

## Ход работы

### Шаг 1: инвентаризация структуры

- Fast Context-субагент + `find/wc/grep`. Крупные модули: `config.py` 678,
  `llm/extract.py` 654, `scripts/bench/bench_health_report.py` 780 строк.
- В git закоммичены дампы: `scripts/bench/*.txt|.log|.bak`, `tests/_dump_*.txt`,
  тестовые картинки и `.docx` в корне.

### Шаг 2: зависимости — grep импортов vs pyproject

```bash
for pkg in ...; do grep -rE "(import|from) $pkg" src scripts tests -l | wc -l; done
```

- 0 импортов: `pyyaml`, `markdown`, `pydantic-settings`.
- Первый grep с якорем `^` дал ложные нули для `dotenv`/`fitz` — импорты бывают
  внутри `try:` с отступом. Перепроверил без якоря.

### Сложность: двойная система конфигурации

- **Симптом:** есть и `config.py` (678 строк, самописный merge env/json/defaults),
  и пакет `settings/` на 7 файлов, который никто вне себя не импортирует.
- **Гипотезы:** незавершённая миграция.
- **Что пробовал:** `grep -rn "from botkin.settings" src` — только внутренние импорты.
- **Решение (находка):** в `settings/loader.py` баг —
  `_project_root = Path(__file__).parent.parent.parent` указывает на `src/`,
  а не на корень: `config.json` оттуда не читается. Плюс `pydantic-settings`
  объявлен в зависимостях, но не используется — ровно та библиотека, что
  заменила бы самописный лоадер.
- **Урок:** полумиграции хуже отсутствия миграции — теперь два источника правды
  и рассинхрон дефолтов (`num_ctx` 8192 в config.json vs 16384 в `_DEFAULTS`).

### Шаг 3: сверка версий с сетью

- Python 3.14.6 — стабильная (2026-06-10), проект на 3.12.9,
  `requires-python <3.14` блокирует апгрейд.
- Отставания: pydantic 2.10.4 → 2.13.4, instructor 1.7.2 → 1.15.4 (мажорный скачок),
  fastapi 0.136.1 → 0.140.8, aiogram 3.28.2 → 3.30.0, plotly 5 → 6, kaleido 0.2 → 1.x.

### Шаг 4: альтернативные локальные VLM

По свежим публичным бенчам (см. Материалы): `qwen2.5vl:7b` — лучший баланс
(~35 с/страница), `qwen3-vl:30b` — максимум точности (~73 с/страница),
`glm-ocr`/`deepseek-ocr` — быстрые OCR-специалисты (~10 с/страница) для первой
ступени двухступенчатого пайплайна, `minicpm-v:8b` — галлюцинирует числа, исключён.

### Шаг 5: REVIEW_PLAN.md

8 фаз: базовая линия → чистка артефактов → зависимости/Python → единая
конфигурация → промты в ресурсы → академическая структура → e2e с метриками
(tokens/sec из `eval_count`/`eval_duration` Ollama, структурный diff
MISSING/EXTRA/MISMATCH, отчёты в `benchmarks/`) → оптимизация моделей
(двухступенчатый OCR-пайплайн, KV-cache q8_0, RAG-валидация через ФСЛИ,
few-shot из верифицированной истории той же клиники, self-consistency).

## Архитектурные решения

### Решение: план как последовательность проверяемых шагов, а не список советов

- **Альтернативы:** свободное эссе-ревью / диф-патчи сразу.
- **Выбрано:** формат «Цель → Файлы → Действия → Приёмка → Проверка» на каждый шаг —
  легковесная модель выполняет шаг изолированно и сама верифицирует результат командой.
- **Компромисс:** объём документа больше; часть шагов исполнитель может счесть очевидными.
- **Когда пересмотреть:** если исполнитель будет путаться в зависимостях между фазами —
  добавить явный граф зависимостей шагов.

## Итог

- `REVIEW_PLAN.md` написан: 8 фаз, ~25 шагов с критериями приёмки. Критерий успеха выполнен.
- Правки кода не выполнялись (по постановке — только план).
- Уроки: (1) grep импортов с `^`-якорем пропускает отступные импорты;
  (2) главный техдолг проекта — не отсутствие структуры, а незавершённые миграции
  (settings/, repos-фасад) и накопленные bench-дампы в git.

## Материалы

- [Python 3.14.6 release](https://www.python.org/downloads/release/python-3146/) — обращение 2026-07-29. Актуальная стабильная версия Python.
- [pydantic HISTORY.md](https://github.com/pydantic/pydantic/blob/main/HISTORY.md) — обращение 2026-07-29. «This release contains the updated `pydantic.v1` namespace... which includes support for Python 3.14» (v2.13.0) — pydantic 2.13 поддерживает Python 3.14.
- [FastAPI 0.140.8 release](https://github.com/fastapi/fastapi/releases/tag/0.140.8) — обращение 2026-07-29. Актуальная версия FastAPI.
- [aiogram 3.30.0 changelog](https://docs.aiogram.dev/en/dev-3.x/changelog.html) — обращение 2026-07-29. Bot API 10.2.
- [instructor CHANGELOG](https://github.com/567-labs/instructor/blob/v1.15.4/CHANGELOG.md) — обращение 2026-07-29. Актуальная 1.15.4.
- [Local Vision-Language OCR Benchmark, nullmirror](https://nullmirror.com/en/blog/2026-05-24-local-vision-language-ocr-benchmark/) — обращение 2026-07-29. «`qwen2.5vl:7b` at 150 DPI is the best speed/accuracy balance» — сравнение qwen2.5vl/qwen3-vl/glm-ocr/minicpm-v по скорости и точности.
- [Best Local Vision Models for Private OCR (2026), MyLocalAI](https://mylocalai.org/blog/best-local-vision-model-ocr) — обращение 2026-07-29. Обзор VLM по VRAM-ярусам, Qwen3-VL как accuracy frontier.
